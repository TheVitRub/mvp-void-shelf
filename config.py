"""
Файл конфигурации для системы мониторинга полок.

Этот модуль содержит все настраиваемые параметры системы мониторинга.
Изменение значений в этом файле позволяет настроить поведение системы
без изменения основного кода.

Параметры:
    YOLO_MODEL: Имя файла модели YOLO (должен находиться в директории MVP/)
    CONFIDENCE_THRESHOLD: Порог уверенности для детекции (0.0 - 1.0)
                         Объекты с уверенностью ниже этого значения игнорируются
    DETECTION_IMG_SIZE: Размер изображения для детекции (пиксели)
                       Рекомендуемые значения: 640, 1280
                       Больше значение = выше точность, но медленнее обработка
    SKIP_FRAMES: Количество кадров для пропуска между обработками
                Используется для оптимизации производительности
                0 = обрабатывать каждый кадр
                7 = обрабатывать каждый 8-й кадр
    MAX_DISPLAY_WIDTH: Максимальная ширина окна для отображения (пиксели)
                      Кадры масштабируются, если ширина превышает это значение
    FRAME_WIDTH: Ширина кадра камеры (пиксели)
    FRAME_HEIGHT: Высота кадра камеры (пиксели)

Рекомендации по настройке:
    - CONFIDENCE_THRESHOLD: Начните с 0.25, увеличьте до 0.5-0.7 для уменьшения ложных срабатываний
    - DETECTION_IMG_SIZE: Используйте 640 для быстрой обработки, 1280 для высокой точности
    - SKIP_FRAMES: Увеличьте до 10-15 для слабых систем, уменьшите до 0-3 для мощных
    - MAX_DISPLAY_WIDTH: Установите в соответствии с разрешением вашего монитора

Автор: [Ваше имя]
Дата: 2026-01-27
"""
import os

YOLO_MODEL = 'best_2026-02-03-12-06.pt'
CONFIDENCE_THRESHOLD = 0.10
DETECTION_IMG_SIZE = 640
SKIP_FRAMES = 7
MAX_DISPLAY_WIDTH = 2000


# Разрешение камеры
FRAME_WIDTH = 2000
FRAME_HEIGHT = 2000

# API настройк
if os.getenv('SERVER') == 'DEV':
    API_BASE_URL = 'http://localhost:8000'
else:
    API_BASE_URL = 'http://100.64.0.8:8000'


ID_STORE = 42013


